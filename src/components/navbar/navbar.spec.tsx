// import { render, screen } from '@testing-library/react';
// import { Navbar } from '.';

describe('example', () => {
  it('should render', () => {
    // render(<Navbar />);
    // const component = screen.getByTestId('example-container');

    expect(1).toBe(1);
  });
});
